#!/usr/bin/env node

/**
 * ElectricalAI Pro N8N Workflow Testing Suite
 * Comprehensive testing of all workflows with realistic data
 */

const axios = require('axios');
const fs = require('fs');
const path = require('path');
const FormData = require('form-data');

const N8N_BASE_URL = process.env.N8N_BASE_URL || 'http://localhost:5678';
const TEST_RESULTS_DIR = path.join(__dirname, '../test-results');
const CONFIG_FILE = path.join(__dirname, '../config/workflow-config.json');

// Test data templates
const testData = {
  estimation: {
    projectId: `TEST-EST-${Date.now()}`,
    projectType: 'residential',
    squareFootage: 2800,
    complexityLevel: 'standard',
    location: 'suburban',
    specialRequirements: ['smart_home_ready', 'ev_charging_prep']
  },
  
  floorPlan: {
    projectId: `TEST-FP-${Date.now()}`,
    metadata: {
      projectType: 'residential',
      squareFootage: 2800,
      description: 'Two-story residential home with modern electrical requirements'
    }
  },
  
  necCompliance: {
    projectId: `TEST-NEC-${Date.now()}`,
    projectType: 'residential',
    squareFootage: 2800,
    roomLayout: [
      {
        name: 'Living Room',
        type: 'living',
        dimensions: { length: 20, width: 16, area: 320 }
      },
      {
        name: 'Kitchen',
        type: 'kitchen',
        dimensions: { length: 14, width: 12, area: 168 }
      },
      {
        name: 'Master Bedroom',
        type: 'bedroom',
        dimensions: { length: 16, width: 14, area: 224 }
      },
      {
        name: 'Master Bathroom',
        type: 'bathroom',
        dimensions: { length: 10, width: 8, area: 80 }
      }
    ],
    electricalElements: [
      {
        type: 'outlet',
        room: 'Living Room',
        coordinates: { x: 120, y: 80, grid: 'B3' },
        specifications: { type: 'Standard', amperage: '15A' }
      },
      {
        type: 'outlet',\n        room: 'Kitchen',\n        coordinates: { x: 200, y: 100, grid: 'D4' },\n        specifications: { type: 'GFCI', amperage: '20A' }\n      },\n      {\n        type: 'outlet',\n        room: 'Master Bathroom',\n        coordinates: { x: 150, y: 60, grid: 'C2' },\n        specifications: { type: 'GFCI', amperage: '15A' }\n      }\n    ],\n    proposedCircuits: [\n      {\n        type: 'lighting',\n        amperage: 15,\n        voltage: 120,\n        serves_rooms: ['Living Room', 'Kitchen'],\n        protection: ['AFCI']\n      },\n      {\n        type: 'small_appliance',\n        amperage: 20,\n        voltage: 120,\n        serves_rooms: ['Kitchen'],\n        protection: ['GFCI']\n      }\n    ],\n    panelSpecifications: {\n      amperage: 200,\n      voltage: 240,\n      circuits: 40\n    }\n  },\n  \n  materialCost: {\n    updateType: 'manual',\n    location: 'suburban',\n    projectId: `TEST-MAT-${Date.now()}`,\n    materials: [\n      {\n        materialId: '12_awg_thhn',\n        supplierPrice: 2.75,\n        quantity: 500,\n        supplier: 'ElectricalSupplyCo',\n        reportedBy: 'test_user'\n      },\n      {\n        materialId: 'outlet_15a_gfci',\n        supplierPrice: 22.50,\n        quantity: 12,\n        supplier: 'ElectricalSupplyCo',\n        reportedBy: 'test_user'\n      },\n      {\n        materialId: 'panel_200a_40circuit',\n        supplierPrice: 320.00,\n        quantity: 1,\n        supplier: 'ElectricalSupplyCo',\n        reportedBy: 'test_user'\n      }\n    ]\n  },\n  \n  projectProgress: {\n    projectId: `TEST-PROG-${Date.now()}`,\n    updateType: 'milestone',\n    phase: 'rough_in',\n    completedTasks: [\n      {\n        taskId: 'conduit_installation',\n        status: 'completed',\n        completedAt: new Date(Date.now() - 24 * 60 * 60 * 1000).toISOString(),\n        assignedTo: 'electrician_1'\n      },\n      {\n        taskId: 'wire_pulling',\n        status: 'completed',\n        completedAt: new Date().toISOString(),\n        assignedTo: 'electrician_2'\n      }\n    ],\n    issues: [\n      {\n        type: 'material_shortage',\n        severity: 'minor',\n        description: 'Delayed delivery of specialty outlets',\n        status: 'open',\n        reportedAt: new Date().toISOString()\n      }\n    ],\n    qualityMetrics: {\n      inspectionResults: [\n        { type: 'conduit_alignment', score: 95, inspector: 'QC_Team' },\n        { type: 'wire_organization', score: 88, inspector: 'QC_Team' }\n      ],\n      complianceScore: 92\n    },\n    timeTracking: {\n      phaseStartDate: new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString(),\n      hoursWorked: 32\n    },\n    costTracking: {\n      actualCost: 8500,\n      budgetedCost: 9200,\n      timeSpent: 5 // days\n    },\n    teamUpdates: [\n      {\n        teamMember: 'electrician_1',\n        tasksAssigned: 8,\n        tasksCompleted: 7,\n        hoursWorked: 16\n      },\n      {\n        teamMember: 'electrician_2',\n        tasksAssigned: 6,\n        tasksCompleted: 6,\n        hoursWorked: 16\n      }\n    ],\n    reportedBy: 'project_manager'\n  }\n};\n\n// Ensure test results directory exists\nif (!fs.existsSync(TEST_RESULTS_DIR)) {\n  fs.mkdirSync(TEST_RESULTS_DIR, { recursive: true });\n}\n\n/**\n * Test electrical estimation workflow\n */\nasync function testEstimationWorkflow() {\n  console.log('🔍 Testing Electrical Estimation Workflow...');\n  \n  try {\n    const startTime = Date.now();\n    \n    const response = await axios.post(\n      `${N8N_BASE_URL}/webhook/electrical-estimation`,\n      testData.estimation,\n      {\n        headers: { 'Content-Type': 'application/json' },\n        timeout: 30000\n      }\n    );\n    \n    const duration = Date.now() - startTime;\n    \n    console.log('✅ Estimation workflow completed successfully!');\n    console.log(`⏱️  Duration: ${duration}ms`);\n    console.log(`💰 Total Cost: $${response.data.estimation?.totalCost || 'N/A'}`);\n    console.log(`⚡ Total Load: ${response.data.technical?.totalLoad || 'N/A'} VA`);\n    \n    return {\n      workflow: 'electrical-estimation',\n      status: 'success',\n      duration,\n      data: response.data,\n      metrics: {\n        totalCost: response.data.estimation?.totalCost,\n        totalLoad: response.data.technical?.totalLoad,\n        accuracy: response.data.accuracy\n      }\n    };\n    \n  } catch (error) {\n    console.error('❌ Estimation workflow failed:', error.message);\n    return {\n      workflow: 'electrical-estimation',\n      status: 'failed',\n      error: error.message,\n      statusCode: error.response?.status\n    };\n  }\n}\n\n/**\n * Test floor plan analysis workflow\n */\nasync function testFloorPlanWorkflow() {\n  console.log('🏠 Testing Floor Plan Analysis Workflow...');\n  \n  try {\n    const startTime = Date.now();\n    \n    // Create test image (1x1 pixel PNG)\n    const testImageBase64 = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==';\n    \n    const form = new FormData();\n    form.append('projectId', testData.floorPlan.projectId);\n    form.append('metadata', JSON.stringify(testData.floorPlan.metadata));\n    \n    const imageBuffer = Buffer.from(testImageBase64, 'base64');\n    form.append('file', imageBuffer, {\n      filename: `test-floor-plan-${testData.floorPlan.projectId}.png`,\n      contentType: 'image/png'\n    });\n    \n    const response = await axios.post(\n      `${N8N_BASE_URL}/webhook/floor-plan-upload`,\n      form,\n      {\n        headers: form.getHeaders(),\n        timeout: 60000\n      }\n    );\n    \n    const duration = Date.now() - startTime;\n    \n    console.log('✅ Floor plan analysis workflow completed successfully!');\n    console.log(`⏱️  Duration: ${duration}ms`);\n    console.log(`🔌 Total Elements: ${response.data.analysis?.totalElements || 'N/A'}`);\n    console.log(`⚡ Estimated Load: ${response.data.analysis?.estimatedLoad || 'N/A'} VA`);\n    \n    return {\n      workflow: 'floor-plan-analysis',\n      status: 'success',\n      duration,\n      data: response.data,\n      metrics: {\n        totalElements: response.data.analysis?.totalElements,\n        estimatedLoad: response.data.analysis?.estimatedLoad,\n        complianceScore: response.data.analysis?.complianceScore\n      }\n    };\n    \n  } catch (error) {\n    console.error('❌ Floor plan analysis workflow failed:', error.message);\n    return {\n      workflow: 'floor-plan-analysis',\n      status: 'failed',\n      error: error.message,\n      statusCode: error.response?.status\n    };\n  }\n}\n\n/**\n * Test NEC compliance workflow\n */\nasync function testNECComplianceWorkflow() {\n  console.log('📋 Testing NEC Compliance Workflow...');\n  \n  try {\n    const startTime = Date.now();\n    \n    const response = await axios.post(\n      `${N8N_BASE_URL}/webhook/nec-compliance-check`,\n      testData.necCompliance,\n      {\n        headers: { 'Content-Type': 'application/json' },\n        timeout: 20000\n      }\n    );\n    \n    const duration = Date.now() - startTime;\n    \n    console.log('✅ NEC compliance workflow completed successfully!');\n    console.log(`⏱️  Duration: ${duration}ms`);\n    console.log(`📊 Compliance Score: ${response.data.compliance?.score || 'N/A'}%`);\n    console.log(`⚠️  Issues Found: ${response.data.issues?.total || 'N/A'}`);\n    \n    return {\n      workflow: 'nec-compliance',\n      status: 'success',\n      duration,\n      data: response.data,\n      metrics: {\n        complianceScore: response.data.compliance?.score,\n        totalIssues: response.data.issues?.total,\n        complianceStatus: response.data.compliance?.status\n      }\n    };\n    \n  } catch (error) {\n    console.error('❌ NEC compliance workflow failed:', error.message);\n    return {\n      workflow: 'nec-compliance',\n      status: 'failed',\n      error: error.message,\n      statusCode: error.response?.status\n    };\n  }\n}\n\n/**\n * Test material cost tracking workflow\n */\nasync function testMaterialCostWorkflow() {\n  console.log('💰 Testing Material Cost Tracking Workflow...');\n  \n  try {\n    const startTime = Date.now();\n    \n    const response = await axios.post(\n      `${N8N_BASE_URL}/webhook/material-cost-update`,\n      testData.materialCost,\n      {\n        headers: { 'Content-Type': 'application/json' },\n        timeout: 15000\n      }\n    );\n    \n    const duration = Date.now() - startTime;\n    \n    console.log('✅ Material cost tracking workflow completed successfully!');\n    console.log(`⏱️  Duration: ${duration}ms`);\n    console.log(`💵 Total Cost: $${response.data.costs?.totalEstimated || 'N/A'}`);\n    console.log(`⚠️  Price Alerts: ${response.data.alerts?.length || 0}`);\n    \n    return {\n      workflow: 'material-cost-tracking',\n      status: 'success',\n      duration,\n      data: response.data,\n      metrics: {\n        totalCost: response.data.costs?.totalEstimated,\n        priceAlerts: response.data.alerts?.length,\n        riskLevel: response.data.costs?.riskLevel\n      }\n    };\n    \n  } catch (error) {\n    console.error('❌ Material cost tracking workflow failed:', error.message);\n    return {\n      workflow: 'material-cost-tracking',\n      status: 'failed',\n      error: error.message,\n      statusCode: error.response?.status\n    };\n  }\n}\n\n/**\n * Test project progress workflow\n */\nasync function testProjectProgressWorkflow() {\n  console.log('📈 Testing Project Progress Workflow...');\n  \n  try {\n    const startTime = Date.now();\n    \n    const response = await axios.post(\n      `${N8N_BASE_URL}/webhook/project-progress-update`,\n      testData.projectProgress,\n      {\n        headers: { 'Content-Type': 'application/json' },\n        timeout: 25000\n      }\n    );\n    \n    const duration = Date.now() - startTime;\n    \n    console.log('✅ Project progress workflow completed successfully!');\n    console.log(`⏱️  Duration: ${duration}ms`);\n    console.log(`📊 Overall Progress: ${response.data.summary?.overallProgress || 'N/A'}%`);\n    console.log(`🎯 Success Probability: ${response.data.summary?.successProbability || 'N/A'}%`);\n    \n    return {\n      workflow: 'project-progress',\n      status: 'success',\n      duration,\n      data: response.data,\n      metrics: {\n        overallProgress: response.data.summary?.overallProgress,\n        successProbability: response.data.summary?.successProbability,\n        currentPhase: response.data.summary?.currentPhase\n      }\n    };\n    \n  } catch (error) {\n    console.error('❌ Project progress workflow failed:', error.message);\n    return {\n      workflow: 'project-progress',\n      status: 'failed',\n      error: error.message,\n      statusCode: error.response?.status\n    };\n  }\n}\n\n/**\n * Test N8N system health\n */\nasync function testSystemHealth() {\n  console.log('🏥 Testing N8N System Health...');\n  \n  try {\n    const response = await axios.get(`${N8N_BASE_URL}/healthz`, {\n      timeout: 5000\n    });\n    \n    console.log('✅ N8N system is healthy');\n    \n    return {\n      test: 'system-health',\n      status: 'success',\n      statusCode: response.status,\n      timestamp: new Date().toISOString()\n    };\n    \n  } catch (error) {\n    console.error('❌ N8N system health check failed:', error.message);\n    \n    return {\n      test: 'system-health',\n      status: 'failed',\n      error: error.message,\n      timestamp: new Date().toISOString()\n    };\n  }\n}\n\n/**\n * Generate comprehensive test report\n */\nfunction generateTestReport(results) {\n  console.log('\\n📋 COMPREHENSIVE WORKFLOW TEST REPORT');\n  console.log('='.repeat(60));\n  \n  const totalTests = results.length;\n  const successfulTests = results.filter(r => r.status === 'success').length;\n  const failedTests = results.filter(r => r.status === 'failed').length;\n  \n  console.log(`Total Tests: ${totalTests}`);\n  console.log(`Successful: ${successfulTests} ✅`);\n  console.log(`Failed: ${failedTests} ❌`);\n  console.log(`Success Rate: ${Math.round((successfulTests / totalTests) * 100)}%`);\n  \n  // Performance metrics\n  const successfulWorkflows = results.filter(r => r.status === 'success' && r.duration);\n  if (successfulWorkflows.length > 0) {\n    const avgDuration = successfulWorkflows.reduce((sum, r) => sum + r.duration, 0) / successfulWorkflows.length;\n    const maxDuration = Math.max(...successfulWorkflows.map(r => r.duration));\n    const minDuration = Math.min(...successfulWorkflows.map(r => r.duration));\n    \n    console.log('\\n⏱️  Performance Metrics:');\n    console.log(`Average Duration: ${Math.round(avgDuration)}ms`);\n    console.log(`Fastest: ${minDuration}ms`);\n    console.log(`Slowest: ${maxDuration}ms`);\n  }\n  \n  // Individual workflow results\n  console.log('\\n📊 Individual Workflow Results:');\n  results.forEach(result => {\n    const status = result.status === 'success' ? '✅' : '❌';\n    const duration = result.duration ? `(${result.duration}ms)` : '';\n    console.log(`${status} ${result.workflow || result.test} ${duration}`);\n    \n    if (result.status === 'failed') {\n      console.log(`    Error: ${result.error}`);\n    } else if (result.metrics) {\n      const metrics = Object.entries(result.metrics)\n        .filter(([_, value]) => value !== undefined && value !== null)\n        .map(([key, value]) => `${key}: ${value}`)\n        .slice(0, 3);\n      if (metrics.length > 0) {\n        console.log(`    Metrics: ${metrics.join(', ')}`);\n      }\n    }\n  });\n  \n  // Recommendations\n  console.log('\\n💡 Recommendations:');\n  if (successfulTests === totalTests) {\n    console.log('🎉 All workflows are functioning perfectly!');\n    console.log('✅ System is ready for production use');\n    console.log('📊 Consider setting up monitoring and alerts');\n  } else {\n    console.log('⚠️  Some workflows need attention:');\n    results.filter(r => r.status === 'failed').forEach(r => {\n      console.log(`   - Fix ${r.workflow || r.test}: ${r.error}`);\n    });\n    console.log('🔧 Review workflow configurations and dependencies');\n    console.log('🔍 Check N8N logs for detailed error information');\n  }\n  \n  // Save detailed report\n  const report = {\n    timestamp: new Date().toISOString(),\n    summary: {\n      totalTests,\n      successfulTests,\n      failedTests,\n      successRate: Math.round((successfulTests / totalTests) * 100)\n    },\n    performance: successfulWorkflows.length > 0 ? {\n      averageDuration: Math.round(successfulWorkflows.reduce((sum, r) => sum + r.duration, 0) / successfulWorkflows.length),\n      fastestDuration: Math.min(...successfulWorkflows.map(r => r.duration)),\n      slowestDuration: Math.max(...successfulWorkflows.map(r => r.duration))\n    } : null,\n    results,\n    environment: {\n      n8nUrl: N8N_BASE_URL,\n      nodeVersion: process.version,\n      platform: process.platform\n    }\n  };\n  \n  const reportPath = path.join(TEST_RESULTS_DIR, `comprehensive-test-report-${Date.now()}.json`);\n  fs.writeFileSync(reportPath, JSON.stringify(report, null, 2));\n  \n  console.log(`\\n📄 Detailed report saved to: ${reportPath}`);\n  \n  return report;\n}\n\n/**\n * Main test execution function\n */\nasync function runAllTests() {\n  console.log('🚀 ElectricalAI Pro N8N Comprehensive Workflow Testing');\n  console.log('='.repeat(70));\n  console.log(`🌐 N8N URL: ${N8N_BASE_URL}`);\n  console.log(`📁 Results Dir: ${TEST_RESULTS_DIR}`);\n  console.log('');\n  \n  const results = [];\n  \n  // Test system health first\n  const healthResult = await testSystemHealth();\n  results.push(healthResult);\n  console.log('');\n  \n  if (healthResult.status === 'failed') {\n    console.error('❌ System health check failed. Aborting workflow tests.');\n    generateTestReport(results);\n    process.exit(1);\n  }\n  \n  // Test all workflows\n  const workflowTests = [\n    testEstimationWorkflow,\n    testFloorPlanWorkflow,\n    testNECComplianceWorkflow,\n    testMaterialCostWorkflow,\n    testProjectProgressWorkflow\n  ];\n  \n  for (const testFunction of workflowTests) {\n    const result = await testFunction();\n    results.push(result);\n    console.log('');\n    \n    // Small delay between tests\n    await new Promise(resolve => setTimeout(resolve, 1000));\n  }\n  \n  // Generate comprehensive report\n  const report = generateTestReport(results);\n  \n  // Exit with appropriate code\n  const successRate = report.summary.successRate;\n  if (successRate === 100) {\n    console.log('\\n🎉 All tests passed! System is ready for production.');\n    process.exit(0);\n  } else if (successRate >= 80) {\n    console.log('\\n⚠️  Most tests passed, but some issues need attention.');\n    process.exit(1);\n  } else {\n    console.log('\\n❌ Multiple test failures detected. System needs attention.');\n    process.exit(1);\n  }\n}\n\n// Execute tests if run directly\nif (require.main === module) {\n  runAllTests().catch(error => {\n    console.error('❌ Test execution failed:', error.message);\n    process.exit(1);\n  });\n}\n\nmodule.exports = {\n  runAllTests,\n  testEstimationWorkflow,\n  testFloorPlanWorkflow,\n  testNECComplianceWorkflow,\n  testMaterialCostWorkflow,\n  testProjectProgressWorkflow,\n  testSystemHealth\n};